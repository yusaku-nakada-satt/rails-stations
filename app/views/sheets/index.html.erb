<!DOCTYPE html>
<html>
  <div class="screen">
    スクリーン　1
  </div>
  <div class = "clients">
    <table>
      <%= @rows.each do |row| %>
      <tr>
          <%= @columns.each do |column| %>
          <td>
            <%= "#{row.row}-#{column.column}" %>
          </td>
          <% end %>
      </tr>
      <% end %>
    </table>
  </div>
</html>